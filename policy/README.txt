Place `security-control-policy.pdf` in this folder.

The application embeds it into a local FAISS vector index (stored under `data/`)
so the LLM can retrieve and cite policy text when producing the gap assessment.

